Left python codes for graphs. Simulation is under C code.

Just run 
1 - make
2 - ./simulation <experiment_count> <D> <L>
If <experiment_count> == 0, it will run for all requested experiment counts.

Example: make; ./simulation 0 2 1;